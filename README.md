# YouTube Fragment Downloader Bot

Telegram бот для скачивания фрагментов видео с YouTube.

## Возможности

- 📥 Скачивание фрагментов видео с YouTube по временным меткам
- ⏱️ Поддержка форматов времени: `HH:MM:SS` или `MM:SS`
- 🎬 Автоматическое объединение видео и аудио в формат MKV
- 🚀 Готов к деплою на Railway

## Использование

### Команды бота

- `/start` - Начать работу с ботом
- `/download <URL> <время_начала-время_конца>` - Скачать фрагмент

### Примеры

```
/download https://www.youtube.com/live/oxfbPqnuYac?si=DoEWSHVspA4YwhS 02:21:15-02:21:50
```

Или просто отправьте сообщение:
```
https://www.youtube.com/live/oxfbPqnuYac?si=DoEWSHVspA4YwhS 02:21:15-02:21:50
```

## Установка и запуск локально

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd trubabot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Установите `ffmpeg` (необходим для yt-dlp):
   - Windows: Скачайте с [ffmpeg.org](https://ffmpeg.org/download.html) и добавьте в PATH
   - Linux: `sudo apt-get install ffmpeg`
   - macOS: `brew install ffmpeg`

4. Создайте файл `.env` и добавьте токен бота:
```
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

5. Запустите бота:
```bash
python bot.py
```

## Деплой на Railway

1. Создайте аккаунт на [Railway](https://railway.app)

2. Создайте новый проект и подключите репозиторий

3. Добавьте переменную окружения:
   - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота

4. Railway автоматически определит Python проект и установит зависимости

5. Важно: Railway должен автоматически установить `ffmpeg` через Nixpacks, но если возникнут проблемы, можно добавить `nixpacks.toml`:

```toml
[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
```

## Получение токена бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

## Технологии

- Python 3.11
- python-telegram-bot - библиотека для работы с Telegram Bot API
- yt-dlp - инструмент для скачивания видео с YouTube
- ffmpeg - для обработки видео и аудио

## Ограничения

- Максимальный размер отправляемого видео: 50 MB (ограничение Telegram)
- Для больших файлов потребуется альтернативный способ доставки

## Лицензия

MIT

