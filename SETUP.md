# Пошаговая инструкция по настройке и запуску бота

## Шаг 1: Создание Telegram бота и получение токена

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "YouTube Fragment Downloader")
4. Введите username для бота (должен заканчиваться на `bot`, например: `my_youtube_downloader_bot`)
5. **Скопируйте токен**, который выдаст BotFather (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Подготовка локального окружения (для тестирования)

### 2.1. Установите Python
- Убедитесь, что у вас установлен Python 3.11 или выше
- Проверьте: `python --version` или `python3 --version`

### 2.2. Установите ffmpeg
- **Windows:**
  - Скачайте с https://ffmpeg.org/download.html
  - Распакуйте и добавьте папку `bin` в переменную PATH
  - Или используйте: `winget install ffmpeg`
- **Linux (Ubuntu/Debian):**
  ```bash
  sudo apt-get update
  sudo apt-get install ffmpeg
  ```
- **macOS:**
  ```bash
  brew install ffmpeg
  ```

### 2.3. Установите зависимости проекта
```bash
pip install -r requirements.txt
```

### 2.4. Создайте файл .env
Создайте файл `.env` в корне проекта со следующим содержимым:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### 2.5. Запустите бота локально
```bash
python bot.py
```

Если все работает, вы увидите сообщение: `Бот запущен...`

### 2.6. Протестируйте бота
1. Найдите вашего бота в Telegram по username
2. Отправьте команду `/start`
3. Попробуйте скачать фрагмент:
   ```
   /download https://www.youtube.com/live/oxfbPqnuYac?si=DoEWSHVspA4YwhS 02:21:15-02:21:50
   ```

## Шаг 3: Деплой на Railway

### 3.1. Создайте аккаунт на Railway
1. Перейдите на https://railway.app
2. Нажмите "Login" и войдите через GitHub (или другой способ)
3. Подтвердите авторизацию

### 3.2. Создайте новый проект
1. На главной странице Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"** (или "Empty Project" если репозиторий еще не создан)

### 3.3. Подключите репозиторий GitHub
Если репозиторий еще не создан:
1. Создайте репозиторий на GitHub
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш_username/trubabot.git
   git push -u origin main
   ```

Если репозиторий уже есть:
1. В Railway выберите ваш репозиторий из списка
2. Railway автоматически начнет деплой

### 3.4. Добавьте переменную окружения
1. В проекте Railway откройте вкладку **"Variables"**
2. Нажмите **"New Variable"**
3. Добавьте:
   - **Name:** `TELEGRAM_BOT_TOKEN`
   - **Value:** ваш токен от BotFather
4. Нажмите **"Add"**

### 3.5. Проверьте деплой
1. Railway автоматически определит Python проект
2. Установит зависимости из `requirements.txt`
3. Установит ffmpeg через nixpacks.toml
4. Запустит бота

### 3.6. Проверьте логи
1. В Railway откройте вкладку **"Deployments"**
2. Выберите последний деплой
3. Откройте вкладку **"Logs"**
4. Убедитесь, что видите: `Бот запущен...`

### 3.7. Протестируйте бота на Railway
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Попробуйте скачать фрагмент видео

## Шаг 4: Решение возможных проблем

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"
**Решение:** Проверьте, что переменная окружения добавлена в Railway правильно

### Проблема: "ffmpeg не найден"
**Решение:** 
- Локально: убедитесь, что ffmpeg установлен и в PATH
- Railway: проверьте, что файл `nixpacks.toml` присутствует в репозитории

### Проблема: "Ошибка при скачивании"
**Решение:**
- Проверьте правильность URL
- Проверьте формат времени (HH:MM:SS-HH:MM:SS)
- Убедитесь, что видео доступно

### Проблема: "Файл слишком большой"
**Решение:** Выберите более короткий фрагмент (Telegram лимит: 50 MB)

## Шаг 5: Дополнительные настройки (опционально)

### Настройка домена (если нужно)
1. В Railway откройте вкладку **"Settings"**
2. В разделе **"Domains"** можно добавить свой домен

### Мониторинг
- Railway автоматически перезапускает бота при сбоях
- Логи доступны в реальном времени в разделе "Deployments"

## Готово! 🎉

Ваш бот должен работать на Railway. Если возникнут проблемы, проверьте логи в Railway или напишите мне.

